<%# if !params[:post][:user_id].blank? %>

<%# end %>
if ( $('body').is('#show') ) {
    window.location = '/user/<%= current_user.username %>'
}
else {
    $('#left-wrapper-<%= @post.id %>').parents('.wrapper').remove()
}